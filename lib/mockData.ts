import type { AuctionResult } from './types';

/**
 * No third-party listings (Invaluable/LiveAuctioneers) — legal/compliance.
 * Search results can be populated later from user submissions or licensed data.
 */
export const mockResults: AuctionResult[] = [];
